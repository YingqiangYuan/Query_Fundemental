# 课程简介

一门 **只讲 SELECT** 的 SQL 实战课，由 23 节自包含的小课组成，每节用一个真实行业 + 一份预生成的 SQLite 数据库。覆盖过滤、排序、聚合、JOIN、子查询、CTE、集合运算、窗口函数，外层套上 learn-this-project 方法论（六个交互式 skill）。

学完之后，你能把中文业务问题反向写成正确的 SELECT 查询，在面试场景里为课程的每个设计决策做辩护，并把这份仓库改造成自己 GitHub 上的作品集版本。
