# Lesson 15 — LEFT JOIN

> INNER JOIN 只会留下"两边都对得上"的行。但很多业务问题恰恰要问的是"那些没对上的怎么办"——这就是 LEFT JOIN 出场的时刻。

## 我们要学什么

这节课的主线是 **LEFT JOIN**——以及它最经典的用法："找出 A 里那些在 B 里没出现过的行"。

| 构件 | 一句话 |
|------|--------|
| `LEFT JOIN` | 保留左表的每一行，即使右表没有匹配 |
| `LEFT JOIN ... WHERE r.<key> IS NULL` | 反连接（anti-join）——"A 有、B 没有"的标准写法 |
| `COUNT(r.<col>)` vs `COUNT(*)` | LEFT JOIN 下数行最常踩的坑 |
| `INNER JOIN` vs `LEFT JOIN` | 同一份数据，换 JOIN 类型，行数差多少 |
| `RIGHT JOIN` | 存在，但实践中几乎不用——总写成"LEFT + 调换两表" |

这一节课结束后，你应该能一眼看出"这个业务问题该用 LEFT JOIN"，并避开 `COUNT(*)` 把零变一这个最常见的 bug。

## 数据库故事

我们假装在运营一家小型网约车平台。系统里有两类东西：**司机**和**车程**。

| 表名 | 内容 | 行数 |
|------|------|------|
| `drivers` | 每位注册司机的资料（姓名、注册日期、城市、车型） | 15 |
| `rides` | 每一次完整的接单记录（司机 ID、乘客名、上下车时间、距离、车费） | 50 |

**关键设定**：有 3 位司机**刚刚注册、还没接过任何一单**（Kenji、Rachel、Andre）。这是真实业务里非常常见的状态——刚通过审核、还没破零的新司机。我们这节课很多例子都围绕"怎么把这些零单司机也查出来"展开。

`rides.driver_id` 是指向 `drivers.driver_id` 的外键。

## 打开数据库

`db.sqlite` 已经**预先生成好并提交进了 git**，就在本目录下，你**不需要自己跑生成脚本**。
直接把它用 **DBeaver**（图形界面）或者命令行 `sqlite3` 打开就行。
不会的话回 [examples/01](../01_sharpen_your_tools/) 复习一下怎么连。

> **如果 `db.sqlite` 不小心坏了**：删掉它，再跑一次 [gen_db.py](./gen_db.py) 就能重新生成。
>
> ```bash
> python gen_db.py
> ```

## 先自己探索一下

正式开始查询之前，**强烈建议你自己先点一点**：

- 在 DBeaver 左侧栏里展开 `drivers` 和 `rides`，双击预览数据。
- 看看 `rides` 里 `driver_id` 这一列出现了哪些值。
- **特别留意**：`drivers` 里有 15 个人，但 `rides.driver_id` 里其实只出现过 12 个不同的司机 ID——那剩下 3 个就是这节课的主角。

带着"哪几个司机没接过单"这个具体问题去看数据，比直接看 SQL 容易记很多。

## 跟着例子练

依次打开下面 5 个 SQL 文件，**一条一条**复制到你的 SQL 编辑器里执行：

- 每个文件最上面都有一段注释，告诉你这条 SQL 想回答**什么业务问题**、用了哪些写法、**为什么**这么写。
- **先读注释**，预想一下结果。
- **运行**它，看真实结果。
- 看完结果再回头读一遍注释，对照一下是不是和你想的一样。

| 文件 | 学到什么 |
|------|----------|
| [example_01.sql](./example_01.sql) | `LEFT JOIN` 基本用法——每位司机 + 他的车程，没接过单的也留下 |
| [example_02.sql](./example_02.sql) | `LEFT JOIN ... WHERE r.ride_id IS NULL`——反连接，找出"还没破零的司机" |
| [example_03.sql](./example_03.sql) | `COUNT(r.ride_id)` vs `COUNT(*)`——LEFT JOIN 下数行最常见的 bug |
| [example_04.sql](./example_04.sql) | 同一份数据，`INNER JOIN` 和 `LEFT JOIN` 数出来的司机数差几个 |
| [example_05.sql](./example_05.sql) | `RIGHT JOIN` 简介——能用，但实践中几乎总写成 LEFT |

## 关于 SQL 注释（小知识）

你会发现每个 example 文件里都有一大堆以 `--` 开头的文字。
SQL 有两种注释写法：

```sql
-- 单行注释：从 -- 到这一行结尾的内容都被数据库忽略。

/*
 * 块注释：可以跨多行。
 * 写长一点的说明很方便。
 */
```

数据库**会直接忽略**注释，所以你随便写，**不会影响**查询结果。
我们在每个 example 文件里把"业务问题"和"为什么这么写"都用注释写在 SQL 旁边，
就是希望你以后回过头来看也能立刻想起这条 SQL 在干嘛——
注释和代码住在一起，比单独翻教程要省事得多。
